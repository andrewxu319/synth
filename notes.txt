component structure:

Synthesizer (not component)
    Voice (not component)
        Chain (not component)
            Mixer
                Gain
                    Oscillators
                        Sine
                        Square
                        Sawtooth
                        Triangle
                        


current version of main.py can be turned into more advanced mono synth

git add --all
git commit -m [notes]
git push origin [name]



oscillators library
each with one equation?
options
- files in a folder
- file with class
- dictionary