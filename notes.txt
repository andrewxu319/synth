component structure:

Synthesizer (not component)
    Voice (not component)
        Chain (not component)
            Mixer
                Gain
                    Oscillator


current version of main.py can be turned into more advanced mono synth

